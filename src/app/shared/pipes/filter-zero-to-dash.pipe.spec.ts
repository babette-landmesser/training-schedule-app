import { FilterZeroToDashPipe } from './filter-zero-to-dash.pipe';

describe('FilterZeroToDashPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterZeroToDashPipe();
    expect(pipe).toBeTruthy();
  });
});
