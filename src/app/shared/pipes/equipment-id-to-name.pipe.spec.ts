import { EquipmentIdToNamePipe } from './equipment-id-to-name.pipe';

describe('EquipmentIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
